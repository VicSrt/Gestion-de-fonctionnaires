#include <stdio.h>
#include"projet2.h"



int main()
{
    structComplet* all=initialisation(100,83);
    charger(all,100);
   // nombre_conflit(all);
    //afficher_entre(all,100);
    //supprimer_entre(all);
   //taille_conflit_moyen(all);
   // supprimer(all*);
}